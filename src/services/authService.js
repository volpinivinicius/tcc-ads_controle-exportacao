/**
 * Auth Service
 *
 * Contains the business logic related to authentication,
 * including credential validation and JWT generation.
 *
 * Verifies the provided User credentials against the stored
 * data and issues a signed token that identifies the User and
 * their AccessRole, keeping this logic isolated from the
 * Auth Controller.
 */