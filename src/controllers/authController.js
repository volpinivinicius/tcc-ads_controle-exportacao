/**
 * Auth Controller
 *
 * Handles user authentication, including login and, when
 * applicable, registration of new Users.
 *
 * Validates the provided credentials against the stored User
 * data and, upon success, issues a JWT that identifies the
 * authenticated User and their AccessRole in subsequent requests.
 */